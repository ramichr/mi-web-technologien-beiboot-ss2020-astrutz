{
  "name": "dev",
  "version": "1.0.0",
  "description": "The frontend dev enviroment can be used to develop using the server API for image data. It should be used to develop feature #7.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "watch": "node reload.js",
    "dev": "docker-compose -f .docker/dev/docker-compose.yml up --build",
    "prod": "docker-compose -f .docker/prod/docker-compose.yml up",
    "babel": "babel src/js/main.js -o dist/main.min.js"
  },
  "author": "Alexander Strutz",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.5",
    "@babel/core": "^7.10.5",
    "@babel/preset-env": "^7.10.4",
    "babel-preset-minify": "^0.5.1"
  }
}
